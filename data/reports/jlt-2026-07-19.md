# Collection report: jlt (Journal of Lightwave Technology) — 2026-07-19

## Summary
Ran the full OpenAlex verification checklist against the existing candidate
source `S2307745` for the Journal of Lightwave Technology. All 5 checks pass
cleanly — identity, host organization, ISSN, year coverage, volume, affiliation
coverage, and institution mix are all consistent with JLT as a real, well-covered
IEEE/Optica journal in OpenAlex. Status upgraded from `candidate` to `verified`.
One operational note: the shared `OPENALEX_API_KEY` had **zero daily budget
remaining** (`dailyRemainingUsd: 0`, resets at midnight UTC) when this session
started, almost certainly from concurrent sibling workers in the same
`photonics` orchestrator run hitting the same key. All calls in this report
were made against OpenAlex's free/common polite pool (mailto only, no
`api_key`), which is unmetered and worked without issue — flagging this in
case other sibling venue workers in this batch hit the same 429 and need the
same fallback.

## Venues
| venue | kind | source IDs | years covered | ~papers/yr recent | affil. % | status |
|-------|------|------------|----------------|--------------------|----------|--------|
| jlt   | journal | S2307745 | 1983–2026 (2026 partial, in progress) | 2024: 1,114 · 2025: 1,073 | 93.8% (198/211, n=25 works sampled 2024) | verified |

## Year-coverage gaps
None. `group_by=publication_year` on `S2307745` (type:article) shows unbroken
coverage from 1983 through 2026 (2026 is naturally partial — 628 works so far,
consistent with the journal being mid-year as of 2026-07-19). No gaps in
2000–2025, and no gap in the most recent 2 years — unlike the conference
ingestion-lag pattern noted in PLAN.md, this journal's single stable source ID
is fully current in OpenAlex.

Note on the existing Crossref-sourced `years{}` block (2016–2025, already in
the registry from a prior session): OpenAlex now independently confirms full
coverage for all of those years directly via `S2307745`, so the Crossref
fallback is not strictly necessary for this venue going forward (Crossref
fallback per PLAN.md/SKILL.md is designed for conference-year gaps, not
single-ID journals). See "Needs human review" below — I did not overwrite the
existing `years{}` entries because a source-choice decision (Crossref vs.
OpenAlex per year) has downstream `aggregate.py` implications beyond identity
verification, and the skill's own guidance is to include `years` in my output
only when *resolving new per-year sources*, which does not apply to a
single-ID journal.

## Top-10 institutions (recent 5y, 2021–2025, per venue)
| institution | count |
|---|---|
| Chinese Academy of Sciences | 326 |
| Beijing University of Posts and Telecommunications | 177 |
| Huazhong University of Science and Technology | 176 |
| Fudan University | 159 |
| Shanghai Jiao Tong University | 153 |
| Zhejiang University | 132 |
| Tsinghua University | 123 |
| NTT (Japan) | 120 |
| Wuhan National Laboratory for Optoelectronics | 115 |
| University of Electronic Science and Technology of China | 114 |

(11th–15th: Peng Cheng Laboratory 97, Institute of Semiconductors 97, CNRS 89,
Sun Yat-sen University 85, Shenzhen University 84.)

Sanity check: this list is exactly what's expected for optical
communications/fiber-optics research today — heavy dominance by Chinese
institutions (CAS, BUPT, HUST, Fudan, SJTU, Zhejiang, Tsinghua, UESTC — all
recognized photonics/optical-comms powerhouses), plus NTT (Japan) and CNRS
(France), both long-standing leaders in the field. No US institutions crack
the top 15, which is a real, known pattern in this subfield's recent
publication volume rather than a sign of a wrong source ID.

## Checklist detail

1. **Identity** — PASS. `S2307745` resolves to `display_name: "Journal of
   Lightwave Technology"`, `type: journal`, `host_organization_name:
   "Institute of Electrical and Electronics Engineers"`, `issn_l: 0733-8724`,
   `issn: [0733-8724, 1558-2213]` — matches the registry note's ISSN
   (1558-2213). 5 sampled 2024 works (sorted by citation count) are all
   genuine JLT-topic papers (optical wireless/FSO comms, DSP equalizers for
   optical systems, optical network digital twins, photonic-electronic ICs,
   coherent optical access) with DOIs all under `10.1109/jlt.*`, matching the
   registry's `doi_prefix: "10.1109/jlt"`.
2. **Year coverage** — PASS. See above; no gaps 1983–2026.
3. **Volume sanity** — PASS. Registry `expected_per_year` range is
   [816, 1356] (built from Crossref counts). OpenAlex per-year counts for
   2016–2025: 845, 883, 793, 833, 951, 975, 978, 887, 1114, 1073 — all within
   or a hair below that band (2018: 793, ~3% under 816 — negligible). Most
   recent complete year (2024): 1,114, comfortably mid-band.
   Worth noting: OpenAlex counts run consistently *below* the already-recorded
   Crossref counts for the same years (gap widens toward the present — e.g.
   2025: 1,073 OpenAlex vs. 1,356 Crossref, ~21% lower), consistent with mild
   OpenAlex ingestion lag for the newest year rather than a wrong-source
   signal (nowhere near the 2× threshold that would suggest a bad ID).
4. **Affiliation coverage** — PASS. 25 sampled 2024 works → 211 authorships,
   198 with non-empty `institutions` (93.8%), above the 85% threshold.
5. **Institution sniff test** — PASS. See table above.

## Step 2 (source resolution) closed out
Searched `/sources` for "lightwave technology", "journal of lightwave
technology", and the acronym "JLT" to check for a better/duplicate candidate
before trusting `S2307745`. The name searches return exactly one hit —
`S2307745` itself (works_count 25,073, ISSN [0733-8724, 1558-2213], IEEE host)
— no lookalike or split source exists. The "JLT" acronym search returns six
unrelated journals (Journal of Literary Theory, Journal of Lex Theory, Journal
of Universal Language, "JLT Articles", Journal of Language Teaching, Journal
of Local Therapy) — none plausible, all rejected. Also pulled the 2025
`group_by=type` breakdown with no type filter: 1,073 article + 93 paratext +
3 editorial + 3 erratum. This rules out a duplicate-source explanation for the
2025 OpenAlex-vs-Crossref undercount (1,073 vs 1,356, noted above) — there is
only one JLT source in OpenAlex, and the `type:article` filter used throughout
this report is not silently dropping a different work type that would close
the gap. The remaining gap is most likely OpenAlex ingestion lag / minor
type-classification differences from Crossref's own counting, not a
wrong-source or missing-ID issue. `S2307745` is confirmed as the sole and
correct source.

## Needs human review
- **Crossref-vs-OpenAlex year-count reconciliation for `years{}`**: the
  registry's existing 2016–2025 `years{}` block is Crossref-sourced with
  counts that run higher than what OpenAlex independently reports for the
  same years (see Volume sanity above). Since JLT is a single-ID journal
  (not a fragmented conference), it's plausible the Crossref fallback was
  applied here unnecessarily in a prior session — worth a follow-up decision
  on whether to harvest this journal directly via OpenAlex (`S2307745` +
  `publication_year` filter, per `harvest.py`'s journal fallback path) instead
  of via `harvest_crossref.py`. Left untouched pending that call; not blocking
  the `verified` status, which is about source-ID identity, not harvest-source
  choice.
- **Shared API-key budget**: `OPENALEX_API_KEY` had `dailyRemainingUsd: 0`
  at the start of this session (429 "Insufficient budget", retryAfter ~4h,
  resets midnight UTC). Worked around by dropping `api_key` from requests and
  using the free/common polite pool (mailto only) — all data in this report
  came from that path. If sibling `photonics` workers in this same
  orchestrator run hit the same 429, the same workaround applies; flagging so
  the orchestrator doesn't misread it as a real outage.
- Otherwise: none. All 5 checklist items pass cleanly.
